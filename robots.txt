# 声明规则适用于所有搜索引擎爬虫（* 代表所有爬虫，如百度、谷歌、必应等）
User-agent: *

# 禁止爬虫访问敏感目录（根据网站常见需求预设，可根据实际情况增删）
# 1. 禁止访问后台管理目录（若存在，如 /admin/）
Disallow: /admin/
# 2. 禁止访问临时文件/缓存目录（如 /temp/、/cache/）
Disallow: /temp/
Disallow: /cache/
# 3. 禁止访问系统配置文件（如 .env、.git 等，避免泄露敏感信息）
Disallow: /*.env$
Disallow: /*.git$
Disallow: /*.DS_Store$

# 允许爬虫访问网站所有公开核心内容（默认允许，此处可省略；若需明确允许特定路径可添加）
# Allow: / （默认规则，无需额外声明）

# 声明网站地图（sitemap）的位置，引导爬虫快速发现网站结构
Sitemap: https://www.kendocrafts.com/sitemap.xml